{
  "name": "@aigov/admin-api",
  "version": "1.0.0",
  "description": "AI FinOps Gateway — Admin API (Control Plane)",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "test": "echo \"Tests pending\" && exit 0"
  },
  "dependencies": {
    "fastify": "^5.3.3",
    "@fastify/cors": "^11.0.0",
    "@fastify/jwt": "^9.0.0",
    "pg": "^8.16.0",
    "bcrypt": "^5.1.1",
    "crypto": "^1.0.1",
    "dotenv": "^16.5.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
